// TODO: Full task manager UI
// Connects to: useTaskStore, useTimer hook
export default function TasksPage() {
  return (
    <div className="p-4 pt-6">
      <h1 className="text-2xl font-semibold mb-1">Tasks</h1>
      <p className="text-sm" style={{ color: 'rgb(var(--muted))' }}>Your daily tasks</p>
      {/* Build: TaskList, AddTaskModal, PomodoroTimer */}
    </div>
  )
}
